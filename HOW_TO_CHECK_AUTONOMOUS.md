# Как проверить, что полет автоматизированный

## ✅ Быстрый тест (без оборудования)

```bash
python simple_autonomous_test.py
```

**Проверяет:**
- ✅ Автоматическую инициализацию
- ✅ Автоматическую выдачу данных
- ✅ Автокалибровку
- ✅ Возврат домой
- ✅ Работу без оператора
- ✅ Обработку ошибок

**Результат:** Если все тесты пройдены → система автономна

---

## 🔍 Признаки автономного полета

### 1. Система запускается автоматически

**Проверка:**
```bash
python drone_navigation.py
```

**Что должно быть:**
- ✅ Запускается одной командой
- ✅ Нет запросов на ввод от пользователя
- ✅ Все модули инициализируются сами
- ✅ Работает в фоновом режиме

### 2. Данные выдаются автоматически

**Проверка в Mission Planner:**

1. Запустите систему:
   ```bash
   python quick_start_matek.py
   ```

2. В Mission Planner:
   - **Data** → **Status** → **Visual Odometry** → "Enabled" и "OK"
   - **Flight Data** → координаты обновляются автоматически

**Признак:** Данные обновляются без участия оператора

### 3. Калибровка автоматическая

**Проверка:**
- При взлете система сама калибруется
- Не требуется ручная установка домашней точки
- Калибровка завершается автоматически за 5-60 секунд

**В логах должно быть:**
```
Автокалибровка запущена
Калибровка завершена:
  Визуальная точка: (320, 240)
  GPS точка: lat=..., lon=...
```

### 4. Работа без оператора

**Тест:**
1. Запустите систему
2. Оставьте работать на 10-30 минут
3. Оператор НЕ должен вмешиваться

**Проверка:**
- Система работает без остановок
- Данные выдаются стабильно
- Нет ошибок или зависаний
- Логи показывают непрерывную работу

---

## 📊 Проверка в Mission Planner

### Индикаторы автономности:

1. **Visual Odometry Status:**
   - "Enabled" ✅
   - "OK" ✅ (данные приходят)
   - Автоматически обновляется

2. **Flight Data:**
   - Координаты обновляются автоматически
   - Позиция стабильна
   - Нет резких скачков

3. **System Status:**
   - Все компоненты "OK"
   - Нет ошибок связи

---

## 🚁 Проверка во время полета

### Тест 1: Зависание (Hover)

**Процедура:**
1. Взлет на высоту 10-20м
2. Режим Hover/Hold
3. **Оператор НЕ управляет дроном**
4. Наблюдать 2-5 минут

**Критерии успеха:**
- ✅ Дрон держит позицию стабильно
- ✅ В Mission Planner видны данные Visual Odometry
- ✅ Нет дрифта или минимальный дрифт (< 5м)

### Тест 2: Возврат домой (RTH)

**Процедура:**
1. Взлет на безопасную высоту
2. Отлет на 50-100м от точки взлета
3. Активировать RTH
4. **Оператор НЕ управляет дроном**

**Критерии успеха:**
- ✅ Дрон возвращается к точке взлета
- ✅ Точность: в пределах 100м от точки взлета
- ✅ Система выдает команды навигации автоматически

---

## 📝 Чеклист автономности

**Перед полетом проверьте:**

- [ ] Система запускается одной командой
- [ ] Нет запросов на ввод от оператора
- [ ] Все модули инициализируются автоматически
- [ ] Тест пройден: `python simple_autonomous_test.py`
- [ ] Visual Odometry показывает "OK" в Mission Planner
- [ ] Данные обновляются автоматически
- [ ] Калибровка работает автоматически
- [ ] Система работает 10+ минут без ошибок

**Во время полета:**

- [ ] Дрон держит позицию без управления оператором
- [ ] Данные Visual Odometry поступают стабильно
- [ ] Возврат домой работает автоматически
- [ ] Нет необходимости в ручной коррекции

---

## 🔬 Детальная проверка

### Логирование работы

```python
# Запустить с логированием
python drone_navigation.py > autonomous_log.txt 2>&1

# Оставить работать 30 минут
# Проверить, что:
# - Нет ошибок
# - Данные выдаются постоянно
# - Нет остановок
```

### Проверка статистики

```python
from drone_navigation import DroneNavigationSystem
import time

nav_system = DroneNavigationSystem(...)
nav_system.start()
nav_system.takeoff()

# Работа 10 минут
time.sleep(600)

# Проверка статистики
stats = nav_system.get_statistics()
print(f"Кадров обработано: {stats['frames_processed']}")
print(f"Средний FPS: {stats['actual_fps']:.1f}")
print(f"Задержка: {stats['last_latency_ms']:.1f}мс")

# Должно быть:
# - frames_processed > 6000 (10 минут * 10 FPS * 60)
# - actual_fps ~ 10
# - last_latency_ms < 100
```

---

## ✅ Итоговая проверка

### Если все проверки пройдены:

✅ **Полет полностью автоматизирован**

**Оператор нужен только для:**
- Включения системы (одна команда)
- Аварийных ситуаций (перехват управления)
- Изменения параметров (редко)

**Система работает автоматически:**
- Стабилизация позиции
- Выдача навигационных данных
- Возврат домой
- Обработка ошибок

---

## 📞 Пример реального сценария

### Автономный полет:

1. **Оператор:** Запускает систему
   ```bash
   python quick_start_matek.py
   ```

2. **Система:** 
   - Автоматически инициализируется
   - Подключается к Matek F405
   - Начинает выдавать данные

3. **Оператор:** Активирует взлет (или автоматически через ArduPilot)

4. **Система:**
   - Автоматически калибруется при взлете
   - Устанавливает домашнюю точку
   - Начинает стабилизацию позиции

5. **Во время полета:**
   - Система работает полностью автономно
   - Выдает данные в ArduPilot каждые 0.1 секунды
   - Стабилизирует позицию автоматически

6. **Возврат домой:**
   - Активируется автоматически или оператором
   - Система выдает команды навигации
   - Дрон возвращается автоматически

7. **Оператор:** Только наблюдает или вмешивается в аварийных случаях

---

**Вывод:** Если система проходит все тесты → полет автоматизированный ✅

