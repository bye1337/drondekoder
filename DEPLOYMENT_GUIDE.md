# Руководство по развертыванию на мини ПК

## 📦 Какие файлы отправлять на мини ПК

### ✅ Обязательные файлы (10 файлов)

```
1. stabilization_processor.py      # Основной модуль стабилизации
2. video_processor.py              # Обработчик видеопотока
3. navigation_server.py            # TCP/UDP сервер
4. gps_integration.py             # GPS интеграция
5. auto_calibration.py            # Автокалибровка
6. home_return.py                 # Возврат домой
7. route_monitor.py               # Мониторинг маршрута
8. matek_integration.py           # Интеграция с Matek F405
9. drone_navigation.py            # Главный модуль
10. requirements.txt             # Зависимости
```

### 📝 Опциональные (для удобства)

```
11. quick_start_matek.py          # Скрипт быстрого запуска
12. MINI_PC_SETUP.md              # Инструкция
13. ARDUPILOT_SETUP.md            # Настройка ArduPilot
14. ardupilot_params.txt          # Параметры
```

### ❌ НЕ нужны (только для разработки)

- `server.py` - веб-сервер (только для отладки)
- `example.py` - примеры
- `test_*.py` - тесты
- `templates/` - HTML шаблоны
- Другая документация (можно оставить только нужную)

---

## 🚀 Варианты развертывания

### Вариант 1: Автоматический (рекомендуется)

```bash
# На разработческом компьютере:
bash create_deployment_package.sh

# Создастся папка deploy_package со всеми нужными файлами
# Отправьте эту папку на мини ПК
```

### Вариант 2: Вручную

Скопируйте файлы из списка выше на мини ПК.

### Вариант 3: Git

```bash
# На мини ПК:
git clone <repository>
cd neridrone111

# Или только нужные файлы через sparse checkout
```

---

## 📤 Отправка файлов на мини ПК

### Способ 1: Через SCP (SSH)

```bash
# Создать архив
cd deploy_package
tar -czf ../deploy.tar.gz *

# Отправить на мини ПК
scp deploy.tar.gz user@mini-pc-ip:/home/user/

# На мини ПК:
cd /home/user
tar -xzf deploy.tar.gz
```

### Способ 2: Через USB

```bash
# На разработческом компьютере:
cd deploy_package
tar -czf ../deploy.tar.gz *

# Скопировать deploy.tar.gz на USB
# На мини ПК: распаковать
tar -xzf deploy.tar.gz
```

### Способ 3: Через Git

```bash
# На мини ПК:
git clone <your-repo-url>
cd neridrone111
# Использовать только нужные файлы
```

---

## 🔧 Установка на мини ПК

### 1. Установить зависимости

```bash
cd /path/to/deployed/files
pip install -r requirements.txt
```

**Или через venv:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Проверить подключения

```bash
# Проверить камеру
ls -l /dev/video*

# Проверить USB-UART адаптер
ls -l /dev/ttyUSB* /dev/ttyACM*
```

### 3. Запустить систему

```bash
# Вариант 1: Быстрый запуск
python quick_start_matek.py

# Вариант 2: Полный запуск
python drone_navigation.py
```

---

## 📋 Чеклист перед отправкой

- [ ] Все обязательные файлы включены
- [ ] requirements.txt актуален
- [ ] Скрипт запуска работает (проверено)
- [ ] Документация включена (опционально)
- [ ] Ненужные файлы исключены

---

## 📊 Размер пакета

**Минимальный набор:** ~50-100 KB (только Python файлы)
**С документацией:** ~200-300 KB

**Архив:** `deploy.tar.gz` ~50-100 KB (сжатый)

---

## ⚙️ Настройка после развертывания

### 1. Настроить параметры

В `quick_start_matek.py` или `drone_navigation.py`:

```python
# Указать ID камеры (обычно 0)
camera_id=0

# Указать порт USB-UART (или None для автопоиска)
serial_port='/dev/ttyUSB0'

# Настроить масштаб под высоту
pixels_to_meters=0.05  # 20 пикселей = 1 метр
```

### 2. Создать systemd service (опционально)

Для автозапуска при загрузке:

```bash
sudo nano /etc/systemd/system/drone-nav.service
```

```ini
[Unit]
Description=Drone Navigation System
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/deployed/files
ExecStart=/usr/bin/python3 /path/to/deployed/files/drone_navigation.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable drone-nav
sudo systemctl start drone-nav
```

---

## 🎯 Итоговый список для отправки

**Обязательно:**
- 10 Python модулей
- requirements.txt

**Рекомендуется:**
- quick_start_matek.py
- MINI_PC_SETUP.md
- ARDUPILOT_SETUP.md

**Итого:** 14 файлов (~200 KB)

---

## 📝 Быстрая команда для создания пакета

```bash
# Создать папку с нужными файлами
bash create_deployment_package.sh

# Создать архив
cd deploy_package
tar -czf ../deploy.tar.gz *

# Отправить на мини ПК
scp deploy.tar.gz user@mini-pc:/home/user/
```

На мини ПК:
```bash
tar -xzf deploy.tar.gz
pip install -r requirements.txt
python quick_start_matek.py
```

Готово! 🚀

