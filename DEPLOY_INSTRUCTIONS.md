# 🚀 Инструкция по загрузке на GitHub

## ✅ Проект готов к загрузке!

Все файлы готовы, репозиторий инициализирован, коммит создан.

## 📤 Как загрузить на GitHub

### Вариант 1: Personal Access Token (Рекомендуется)

1. Создайте Personal Access Token на GitHub:
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Дайте имя: "drondekoder2.0"
   - Выберите срок действия
   - Включите scope: `repo` (все)
   - Нажмите "Generate token"
   - **Скопируйте токен** (он показывается только один раз!)

2. Выполните команду в терминале:

```bash
cd c:\Users\komar\Desktop\drondekoder2.0
git push -u origin develop
```

3. При запросе введите:
   - Username: `bye1337`
   - Password: **вставьте ваш token**

### Вариант 2: SSH (Если настроен SSH ключ)

1. Проверьте SSH ключ:
```bash
ls ~/.ssh/id_rsa.pub
```

2. Если есть, измените remote на SSH:
```bash
cd c:\Users\komar\Desktop\drondekoder2.0
git remote set-url origin git@github.com:bye1337/drondekoder.git
git push -u origin develop
```

### Вариант 3: GitHub Desktop

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Авторизуйтесь с вашим аккаунтом
3. Откройте папку проекта: `File > Add local repository`
4. Выберите: `c:\Users\komar\Desktop\drondekoder2.0`
5. Нажмите "Publish branch" > выберите `develop`

### Вариант 4: VS Code

1. Откройте проект в VS Code
2. Перейдите в Source Control (Ctrl+Shift+G)
3. Нажмите "..." > "Branch" > "Publish Branch"
4. Выберите `develop`

## 🔍 Проверка перед загрузкой

Проект готов:
- ✅ 35 файлов добавлены
- ✅ Коммит создан
- ✅ Ветка `develop` создана
- ✅ Remote настроен

## 📊 Что будет загружено

### Основные файлы
- ✅ Все Python модули (35 файлов)
- ✅ Конфигурации YAML
- ✅ Документация (5 MD файлов)
- ✅ Скрипты и утилиты

### Что НЕ будет загружено (по .gitignore)
- ❌ Модели обученные (`.pth`, `.pt`)
- ❌ Данные для обучения
- ❌ Логи
- ❌ Кэш Python (`__pycache__`)
- ❌ Virtual environments
- ❌ IDE настройки

## 🌐 После загрузки

Ссылка на репозиторий:
https://github.com/bye1337/drondekoder

Ветка `develop`:
https://github.com/bye1337/drondekoder/tree/develop

## 🆘 Если возникают проблемы

### Ошибка "Permission denied"
- Проверьте, что используете правильный токен/пароль
- Убедитесь, что токен имеет scope `repo`

### Ошибка "Branch already exists"
```bash
git fetch origin
git pull origin develop
git push origin develop
```

### Конфликт с существующим репозиторием
Если в репозитории уже есть файлы:
```bash
git pull origin develop --rebase
git push origin develop
```

## 📋 Быстрая справка

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Проверить remote
git remote -v

# Изменить URL если нужно
git remote set-url origin https://github.com/bye1337/drondekoder.git

# Загрузить
git push -u origin develop
```

---

**Проект готов к развёртыванию!** 🎉

Выполните команду `git push` когда будете готовы.

