# Настройка ArduPilot на Matek F405

## 📋 Что нужно настроить

Для работы с визуальной навигацией нужно настроить ArduPilot прошивку на Matek F405.

---

## 🔧 Шаг 1: Подключение к Mission Planner

### Установка Mission Planner

1. Скачайте Mission Planner: https://ardupilot.org/planner/
2. Установите на Windows компьютер
3. Подключите Matek F405 к компьютеру через USB

### Подключение

1. Откройте Mission Planner
2. Выберите COM порт Matek F405
3. Нажмите "Connect"
4. После подключения вы увидите интерфейс дрона

---

## ⚙️ Шаг 2: Настройка последовательного порта (UART2)

### Где настраивать

1. В Mission Planner откройте: **Config/Tuning** → **Full Parameter List**
2. Или нажмите `Ctrl+F` и введите название параметра

### Параметры для UART2:

```
SERIAL2_PROTOCOL = 2        # MAVLink 2 (обязательно!)
SERIAL2_BAUD = 57600        # Скорость (57600 или 115200)
```

**Как установить:**
1. Найдите параметр `SERIAL2_PROTOCOL`
2. Установите значение **2** (MAVLink 2)
3. Найдите параметр `SERIAL2_BAUD`
4. Установите **57600** (или 115200, если используете эту скорость)
5. Нажмите **Write Params** для сохранения

**Важно:** 
- Используйте **UART2** (обычно он свободен)
- Если UART2 занят, можно использовать **UART3** или **UART4**
- Тогда параметры будут: `SERIAL3_PROTOCOL` и `SERIAL3_BAUD`

---

## 📡 Шаг 3: Включение Visual Odometry

### Основные параметры:

```
VISODOM_ENABLE = 1          # Включить Visual Odometry (1 = включено, 0 = выключено)
VISODOM_TYPE = 2            # Тип: 2 = VISION_POSITION_ESTIMATE
VISODOM_POS_LAG = 0.1       # Задержка позиции (секунды)
VISODOM_XY_P = 0.01         # Точность позиции X/Y (метры)
VISODOM_Z_P = 0.1           # Точность позиции Z/высоты (метры)
VISODOM_VEL_P = 0.05        # Точность скорости (м/с)
```

### Пошаговая настройка:

1. В Mission Planner откройте **Config/Tuning** → **Full Parameter List**
2. Найдите `VISODOM_ENABLE` → установите **1**
3. Найдите `VISODOM_TYPE` → установите **2**
4. Найдите `VISODOM_POS_LAG` → установите **0.1**
5. Найдите `VISODOM_XY_P` → установите **0.01**
6. Нажмите **Write Params**

---

## 🎯 Шаг 4: Настройка EKF3 для использования Visual Odometry

EKF3 (Extended Kalman Filter) - это фильтр, который объединяет данные от разных источников.

### Параметры EKF3:

```
EK3_SRC1_POSXY = 6          # Источник позиции X/Y: 6 = Visual Odometry
EK3_SRC1_VELXY = 6          # Источник скорости X/Y: 6 = Visual Odometry
EK3_SRC1_POSZ = 1           # Источник высоты: 1 = Barometer (или 0 = GPS если доступен)
```

**Значения источников:**
- **0** = None (не использовать)
- **1** = Barometer (барометр)
- **3** = GPS
- **6** = Visual Odometry (наша система)
- **7** = Rangefinder

### Настройка:

1. Найдите `EK3_SRC1_POSXY` → установите **6**
2. Найдите `EK3_SRC1_VELXY` → установите **6**
3. Найдите `EK3_SRC1_POSZ` → установите **1** (барометр для высоты)
4. Нажмите **Write Params**

**Примечание:** Высоту лучше брать от барометра, так как наша система работает по X/Y.

---

## 📊 Шаг 5: Дополнительные настройки (опционально)

### Для повышения точности:

```
VISODOM_XY_VAR = 0.1        # Вариация позиции X/Y
VISODOM_Z_VAR = 0.5         # Вариация высоты
VISODOM_VEL_VAR = 0.1       # Вариация скорости
```

### Для фильтрации данных:

```
VISODOM_ALIGN = 0           # Выравнивание (0 = авто)
VISODOM_DELAY_MS = 0        # Дополнительная задержка (миллисекунды)
```

---

## ✅ Шаг 6: Проверка настроек

### В Mission Planner:

1. Перейдите в **Data** → **Status**
2. Найдите раздел **Sensor Status**
3. Должны увидеть:
   - **Visual Odometry**: Enabled
   - **Visual Odometry Data**: OK (когда данные приходят)

### Через телеметрию:

В Mission Planner можно отследить:
- Входящие MAVLink сообщения
- Статус Visual Odometry
- Данные позиции

---

## 🧪 Шаг 7: Тестирование

### Простой тест:

1. Подключите систему навигации к Matek F405
2. Запустите систему:
   ```bash
   python quick_start_matek.py
   ```
3. В Mission Planner проверьте:
   - **Data** → **Status** → **Visual Odometry** должен показать данные
   - **Flight Data** → должны обновляться координаты

### Проверка связи:

В Mission Planner откройте **Terminal** и проверьте, что:
- MAVLink сообщения приходят
- Нет ошибок связи

---

## 📋 Полный список параметров для копирования

```
# Последовательный порт UART2
SERIAL2_PROTOCOL = 2
SERIAL2_BAUD = 57600

# Visual Odometry
VISODOM_ENABLE = 1
VISODOM_TYPE = 2
VISODOM_POS_LAG = 0.1
VISODOM_XY_P = 0.01
VISODOM_Z_P = 0.1
VISODOM_VEL_P = 0.05

# EKF3 - использование Visual Odometry
EK3_SRC1_POSXY = 6
EK3_SRC1_VELXY = 6
EK3_SRC1_POSZ = 1
```

---

## 🔄 Альтернатива: Если используете другой UART

Если UART2 занят, используйте UART3 или UART4:

**Для UART3:**
```
SERIAL3_PROTOCOL = 2
SERIAL3_BAUD = 57600
```

**Для UART4:**
```
SERIAL4_PROTOCOL = 2
SERIAL4_BAUD = 57600
```

В коде тоже нужно изменить:
```python
# Для UART3 нужно изменить в matek_integration.py
# Но обычно UART2 используется
```

---

## ⚠️ Важные замечания

1. **Скорость передачи** должна совпадать:
   - В ArduPilot: `SERIAL2_BAUD = 57600`
   - В коде: `baudrate=57600`

2. **Протокол** обязательно MAVLink 2:
   - `SERIAL2_PROTOCOL = 2` (НЕ 1!)

3. **После изменения параметров:**
   - Нажмите **Write Params**
   - Перезагрузите дрон (или выполните `REBOOT`)

4. **Проверка работы:**
   - Visual Odometry должен показать "Enabled"
   - Данные должны приходить (Status показывает OK)

---

## 🐛 Устранение проблем

### Проблема: Visual Odometry не включается

**Решение:**
1. Проверьте, что `VISODOM_ENABLE = 1`
2. Проверьте, что `SERIAL2_PROTOCOL = 2`
3. Перезагрузите дрон

### Проблема: Данные не приходят

**Решение:**
1. Проверьте физическое подключение
2. Проверьте скорость передачи (57600/115200)
3. Проверьте, что система навигации отправляет данные
4. В Mission Planner → Terminal проверьте MAVLink сообщения

### Проблема: ArduPilot игнорирует данные

**Решение:**
1. Проверьте `EK3_SRC1_POSXY = 6` (Visual Odometry)
2. Проверьте `EK3_ENABLE = 1` (EKF3 должен быть включен)
3. Проверьте статус Visual Odometry в Mission Planner

---

## 📚 Дополнительные ресурсы

- [ArduPilot Visual Odometry](https://ardupilot.org/copter/docs/common-visual-odometry-landing-page.html)
- [ArduPilot EKF3 Configuration](https://ardupilot.org/dev/docs/ekf3-sensor-selection.html)
- [MAVLink Protocol](https://mavlink.io/en/)

---

## ✅ Чеклист настройки

- [ ] Mission Planner установлен и подключен к Matek F405
- [ ] `SERIAL2_PROTOCOL = 2` (MAVLink 2)
- [ ] `SERIAL2_BAUD = 57600` (или 115200)
- [ ] `VISODOM_ENABLE = 1`
- [ ] `VISODOM_TYPE = 2`
- [ ] `EK3_SRC1_POSXY = 6` (Visual Odometry)
- [ ] `EK3_SRC1_VELXY = 6` (Visual Odometry)
- [ ] `EK3_SRC1_POSZ = 1` (Barometer)
- [ ] Параметры сохранены (Write Params)
- [ ] Дрон перезагружен
- [ ] Visual Odometry показывает "Enabled" в Mission Planner
- [ ] Тестовая передача данных работает

После выполнения всех пунктов система готова к работе! 🚀

