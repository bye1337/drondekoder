# Подключение к Matek F405 с мини ПК на Ryzen 7

## 🔌 Варианты подключения

### Вариант 1: USB-UART адаптер (Рекомендуется)

**Необходимо:**
- USB-UART адаптер (CH340, CP2102, FT232 или аналогичный)
- Провода для подключения к Matek F405

**Схема подключения:**
```
Мини ПК (USB)           USB-UART адаптер          Matek F405
USB порт ──────────> [CH340/CP2102] ──────────> UART2
                           │
                      TX ──┼──> RX (UART2)
                      RX ──┼──< TX (UART2)
                      GND ─┼──> GND
```

**Порты на Matek F405:**
- **UART2** - рекомендуется (обычно свободен)
- **UART3** - альтернатива
- **UART4** - альтернатива

### Вариант 2: Прямое подключение (если есть COM порт)

Если у мини ПК есть COM порт:
```
Мини ПК                 Matek F405
COM порт ────────────> UART2
  TX ─────────────────> RX
  RX <───────────────── TX
  GND ────────────────> GND
```

## 📦 Необходимое оборудование

1. **USB-UART адаптер** (если нет COM порта)
   - CH340 (~$2-5)
   - CP2102 (~$3-7)
   - FT232RL (~$5-10)

2. **Провода** для подключения
   - 3 провода: TX, RX, GND
   - Рекомендуется: провода 26-28 AWG

## 🔧 Настройка на мини ПК (Linux)

### 1. Определение порта

```bash
# Вариант 1: Список всех последовательных портов
ls -l /dev/tty* | grep -E "(USB|ACM)"

# Вариант 2: Использовать Python
python3 -c "
import serial.tools.list_ports
ports = serial.tools.list_ports.comports()
for p in ports:
    print(f'{p.device}: {p.description}')
"

# Вариант 3: Проверить dmesg после подключения
dmesg | tail -20 | grep -i tty
```

**Обычные порты на Linux:**
- `/dev/ttyUSB0` - USB-UART адаптеры (CH340, CP2102)
- `/dev/ttyACM0` - USB-UART адаптеры (FTDI)
- `/dev/ttyS0`, `/dev/ttyS1` - встроенные COM порты

### 2. Установка прав доступа

```bash
# Добавить пользователя в группу dialout
sudo usermod -a -G dialout $USER

# Или дать права напрямую
sudo chmod 666 /dev/ttyUSB0  # Замените на ваш порт

# После добавления в группу - перелогиниться
```

### 3. Проверка подключения

```bash
# Установить minicom или screen
sudo apt install minicom screen

# Проверить подключение (Ctrl+A, затем X для выхода)
minicom -D /dev/ttyUSB0 -b 57600

# Или через screen
screen /dev/ttyUSB0 57600
# Ctrl+A, затем K для выхода
```

## 🚀 Использование

### Автоматический поиск порта

Система может автоматически найти USB-UART адаптер:

```python
from drone_navigation import DroneNavigationSystem
from matek_integration import MatekF405Bridge

nav_system = DroneNavigationSystem(...)

# Автоматический поиск порта
bridge = MatekF405Bridge(
    navigation_system=nav_system,
    serial_port=None,  # Автопоиск!
    baudrate=57600
)
```

### Ручное указание порта

Если автопоиск не работает, укажите порт вручную:

```python
bridge = MatekF405Bridge(
    navigation_system=nav_system,
    serial_port='/dev/ttyUSB0',  # Ваш порт
    baudrate=57600
)
```

### Полный пример

```python
from drone_navigation import DroneNavigationSystem
from matek_integration import MatekF405Bridge
import time

# 1. Создать систему навигации
nav_system = DroneNavigationSystem(
    camera_id=0,  # ID камеры на мини ПК
    target_fps=10.0,
    optimize_for_latency=True
)

# 2. Создать мост с Matek F405
bridge = MatekF405Bridge(
    navigation_system=nav_system,
    serial_port=None,  # Автопоиск USB-UART адаптера
    baudrate=57600,
    pixels_to_meters=0.05  # 20 пикселей = 1 метр (для высоты ~50м)
)

# 3. Запустить систему
nav_system.start()

# 4. Подключить к Matek F405
if bridge.start():
    print("✅ Подключено к Matek F405")
    
    # Сигнал взлета
    nav_system.takeoff()
    
    # Основной цикл
    try:
        while True:
            bridge.update()  # Отправка данных в Matek F405
            time.sleep(0.1)  # 10 Гц
    except KeyboardInterrupt:
        print("\nОстановка...")
finally:
    bridge.stop()
    nav_system.stop()
```

## ⚙️ Настройка Matek F405

### Параметры ArduPilot

В Mission Planner или через параметры:

```
# Последовательный порт (UART2)
SERIAL2_PROTOCOL = 2        # MAVLink 2
SERIAL2_BAUD = 57600        # Скорость (или 115200)

# Визуальная навигация
VISODOM_ENABLE = 1          # Включить Visual Odometry
VISODOM_TYPE = 2            # VISION_POSITION_ESTIMATE
VISODOM_POS_LAG = 0.1       # Задержка позиции (секунды)
VISODOM_XY_P = 0.01         # Точность позиции X/Y

# Использование визуальной навигации
EK3_SRC1_POSXY = 6          # Visual Odometry для позиции X/Y
EK3_SRC1_VELXY = 6          # Visual Odometry для скорости X/Y
EK3_SRC1_POSZ = 1           # Barometer для высоты
```

## 🔍 Диагностика

### Проверка подключения

```python
import serial

try:
    ser = serial.Serial('/dev/ttyUSB0', 57600, timeout=1)
    print(f"✅ Порт открыт: {ser.name}")
    print(f"   Baudrate: {ser.baudrate}")
    
    # Попытка чтения
    data = ser.read(100)
    if data:
        print(f"   Получены данные: {data.hex()[:20]}...")
    else:
        print("   Данные не получены (это нормально, если ничего не отправляется)")
    
    ser.close()
except Exception as e:
    print(f"❌ Ошибка: {e}")
```

### Проверка работы системы

```bash
# Запустить с подробным выводом
python3 matek_integration.py

# Ожидаемый вывод:
# ✅ Подключено к /dev/ttyUSB0 на скорости 57600
# ✅ Мост Matek F405 запущен
# Система работает. Нажмите Ctrl+C для остановки
```

## 🐛 Устранение проблем

### Проблема: Порт не найден

**Решение:**
```bash
# 1. Проверить подключение USB-UART адаптера
lsusb | grep -i "serial\|uart\|ch340\|cp210"

# 2. Проверить наличие драйверов
dmesg | grep -i "ch340\|cp210\|ftdi"

# 3. Установить драйверы (если нужно)
sudo apt install usbutils

# 4. Проверить права
sudo chmod 666 /dev/ttyUSB0
```

### Проблема: Permission denied

**Решение:**
```bash
# Добавить пользователя в группу dialout
sudo usermod -a -G dialout $USER

# Перелогиниться или выполнить:
newgrp dialout
```

### Проблема: Порт занят

**Решение:**
```bash
# Найти процесс, использующий порт
sudo lsof /dev/ttyUSB0

# Или
sudo fuser /dev/ttyUSB0

# Остановить процесс
sudo kill -9 <PID>
```

### Проблема: Неправильная скорость

**Проверьте:**
- Скорость в коде должна совпадать со скоростью в ArduPilot
- Обычно: 57600 или 115200 бод

## 📊 Преимущества мини ПК

✅ **Мощность:** Ryzen 7 обеспечивает отличную производительность  
✅ **USB порты:** Много портов для подключения оборудования  
✅ **Стабильность:** Более надежная работа, чем одноплатные компьютеры  
✅ **Возможности:** Легко добавлять дополнительные устройства  

## 📝 Чеклист подключения

- [ ] USB-UART адаптер подключен к мини ПК
- [ ] Адаптер подключен к UART2 на Matek F405 (TX, RX, GND)
- [ ] Порт определен (`/dev/ttyUSB0` или `/dev/ttyACM0`)
- [ ] Права доступа настроены
- [ ] Скорость передачи совпадает (57600/115200)
- [ ] MAVLink настроен в ArduPilot
- [ ] VISODOM включен в ArduPilot
- [ ] Система запущена и отправляет данные

## 💡 Советы

1. **Используйте качественный USB-UART адаптер** - дешевые могут терять данные
2. **Короткие провода** - минимизируйте длину проводов для надежности
3. **Заземление** - обязательно подключите GND
4. **Тестирование** - сначала протестируйте на столе перед полетом

