# Инструкция по отправке в Git

## ✅ Коммит создан локально

Коммит готов в ветке `develop2.0`. 

## 📤 Отправка в репозиторий

### Вариант 1: Если есть права доступа

```bash
git push -u origin develop2.0
```

### Вариант 2: Если нет прав (нужно настроить SSH ключ)

1. **Проверьте SSH ключ:**
```bash
ls -la ~/.ssh/id_rsa.pub
```

2. **Если нет ключа, создайте:**
```bash
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"
```

3. **Добавьте публичный ключ на GitHub:**
```bash
cat ~/.ssh/id_rsa.pub
# Скопируйте вывод и добавьте в GitHub → Settings → SSH Keys
```

4. **Проверьте подключение:**
```bash
ssh -T git@github.com
```

5. **Отправьте ветку:**
```bash
git push -u origin develop2.0
```

### Вариант 3: Через HTTPS (если SSH не работает)

```bash
# Изменить remote на HTTPS
git remote set-url origin https://github.com/NarKomaRick/neridrone111.git

# Отправить
git push -u origin develop2.0

# При запросе введите логин и токен доступа GitHub
```

## 📋 Проверка перед отправкой

```bash
# Проверить текущую ветку
git branch

# Проверить статус
git status

# Посмотреть что будет отправлено
git log origin/develop2.0..HEAD --oneline

# Отправить
git push -u origin develop2.0
```

## 🔍 Что отправляется

**Основные модули:**
- Все Python файлы для стабилизации
- Модули интеграции с Matek F405
- Документация

**Исключено (в .gitignore):**
- venv/
- __pycache__/
- deploy_package/
- Тестовые результаты

## ✅ После отправки

Ветка `develop2.0` будет доступна в репозитории и можно будет:
- Клонировать на мини ПК
- Создать Pull Request (если нужно)
- Использовать для развертывания

