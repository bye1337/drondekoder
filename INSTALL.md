# 📦 Инструкция по установке

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

Или по одной библиотеке:

```bash
pip install torch torchvision torchaudio
pip install opencv-python Pillow albumentations
pip install fastapi uvicorn streamlit
pip install numpy pandas scikit-learn
pip install python-dotenv pydantic aiofiles
pip install scipy matplotlib tqdm
```

## Шаг 2: Проверка установки

Запустите пример для проверки:

```bash
python example.py
```

Если вы видите вывод без ошибок - всё установлено правильно!

## Важно о модели

⚠️ **Модель не обучена!** Это ожидаемо, так как мы только что создали проект.

### Что означает "модель не загружена"?

Когда вы запускаете веб-интерфейс, вы увидите сообщение:
```
⚠️ Модель не загружена
```

Это **НЕ ошибка** - это означает, что модель еще не была обучена на ваших данных.

### Для обучения модели нужны:

1. **Данные** - пары изображений (карта и снимок дрона)
2. **GPU** (рекомендуется) для ускорения обучения
3. **Время** - обучение занимает несколько часов

## Быстрый путь к работающей системе

### Вариант 1: Демо без обучения (для тестирования интерфейса)

Веб-интерфейс работает даже без обученной модели - просто возвращает тестовые координаты:

```bash
streamlit run src/api/web_app.py
```

### Вариант 2: Обучение модели (для реального использования)

#### Шаг 1: Создание датасета

**Вариант А: Использовать реальные данные**
- Загрузите спутниковые карты (Google Earth, Яндекс Карты и т.д.)
- Сделайте снимки дрона или вырежьте фрагменты карт
- Разместите их в `data/processed/pairs/`

**Вариант Б: Создать синтетический датасет**

Скачайте любую большую спутниковую карту и создайте из неё датасет:

```bash
python scripts/create_dataset.py --map-image path/to/large_map.jpg --num-pairs 1000
```

#### Шаг 2: Обучение

```bash
python src/training/train.py --config config/train_config.yaml
```

**Время обучения:**
- На GPU (RTX 3060): ~2-4 часа
- На CPU: ~12-24 часа

#### Шаг 3: Использование

После обучения модель сохранится в `models/checkpoints/best_model.pth`
Весь интерфейс автоматически найдёт её!

## Проверка установки PyTorch

Убедитесь, что PyTorch видит вашу видеокарту:

```python
import torch
print(f"PyTorch version: {torch.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"GPU: {torch.cuda.get_device_name(0)}")
```

## Типичные проблемы

### Ошибка: "No module named 'torch'"

```bash
pip install torch torchvision
```

### Ошибка: Out of memory

Уменьшите batch size в `config/train_config.yaml`:
```yaml
training:
  batch_size: 16  # вместо 32
```

### Ошибка: CUDA out of memory

Используйте CPU или уменьшите размер изображений:
```yaml
model:
  input_size: [256, 256]  # вместо [512, 512]
```

## Минимальные требования

### Для тестирования интерфейса:
- Python 3.8+
- 4GB RAM
- 2GB свободного места

### Для обучения:
- Python 3.8+
- 16GB+ RAM
- NVIDIA GPU с 8GB+ VRAM (рекомендуется)
- 20GB+ свободного места

## Где найти помощь?

1. Прочитайте [QUICKSTART.md](QUICKSTART.md)
2. Изучите [TRAINING_GUIDE.md](TRAINING_GUIDE.md) для обучения
3. Запустите `example.py` для примера
4. Проверьте [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)

---

**Статус:** Проект готов к использованию ✅
**Модель:** Требует обучения (но интерфейс работает без неё!)

