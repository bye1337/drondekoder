# Система стабилизации позиции БАС

**Система выдачи навигационных измерений по оптическим данным подстилающей поверхности по видеопотоку с нижней камеры в целях стабилизации позиции БАС во время зависания.**

## 🎯 Описание

Автономная система навигации БАС, использующая **Optical Flow (Lucas-Kanade)** для отслеживания смещения между последовательными кадрами видеопотока. Система обеспечивает:

- ✅ Стабилизацию позиции при зависании
- ✅ Возврат домой (RTH) с точностью до 100м
- ✅ Интеграцию с GPS/ГЛОНАСС для резервирования
- ✅ Автокалибровку при взлете
- ✅ TCP/UDP сервер для связи с системой управления
- ✅ Задержки <0.1 секунды (целевая частота 10 Гц)

## ✨ Особенности

- ⚡ **Real-time обработка** - 30+ FPS
- 🎯 **Высокая точность** - отслеживание смещения до 1-2 пикселей
- 📹 **Поддержка двух камер** - основная и валидационная
- 💪 **Устойчивость** - медианная фильтрация для устранения выбросов
- 📊 **Метрики стабильности** - отслеживание качества стабилизации

## 📦 Установка

### Требования

- Python 3.8+
- OpenCV 4.8+
- NumPy 1.24+

### Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

## 🚀 Использование

### 1. Простой пример

```bash
python example.py
```

Откроется окно с видео, где вы увидите:
- Зеленый круг - центр кадра (опорная точка)
- Красная стрелка - смещение от центра
- Синий круг - текущая позиция
- Информацию о confidence, FPS, стабильности

**Управление:**
- `q` - выход
- `r` - сброс стабилизатора

### 2. Основная система навигации (для дрона)

```bash
python drone_navigation.py
```

Запускает полную систему:
- Стабилизация позиции по видеопотоку
- TCP/UDP сервер (порты 5001, 5002)
- Автокалибровка при взлете
- Модуль возврата домой
- Интеграция с GPS

### 3. Веб-сервер (для отладки)

```bash
python server.py
```

Сервер запустится на `http://localhost:5000`

#### API Endpoints:

**GET** `/api/status` - статус системы
```bash
curl http://localhost:5000/api/status
```

**POST** `/api/process_frame` - обработка кадра
```bash
# Отправка base64 изображения
curl -X POST http://localhost:5000/api/process_frame \
  -H "Content-Type: application/json" \
  -d '{"frame": "data:image/jpeg;base64,..."}'
```

**POST** `/api/reset` - сброс стабилизатора
```bash
curl -X POST http://localhost:5000/api/reset
```

**POST** `/api/set_route` - установка маршрута
```bash
curl -X POST http://localhost:5000/api/set_route \
  -H "Content-Type: application/json" \
  -d '{"waypoints": [{"x": 100, "y": 100}, {"x": 200, "y": 200}]}'
```

### 3. Использование в Python

```python
from video_processor import VideoProcessor
import cv2

# Создание процессора
processor = VideoProcessor(
    use_dual_camera=False,      # Использовать одну камеру
    primary_method='lucas_kanade'  # Метод стабилизации
)

# Обработка кадра
cap = cv2.VideoCapture(0)
ret, frame = cap.read()

result = processor.process_frame(primary_frame=frame)

if result:
    position = result['position']      # [x, y]
    offset = result['offset']          # смещение от начала
    velocity = result['velocity']      # скорость
    confidence = result['confidence']   # уверенность 0.0-1.0
    
    # Использование для коррекции позиции дрона
    if confidence > 0.7:
        # Позиция стабильна, можно использовать
        print(f"Позиция: {position}")
        print(f"Смещение: {offset}")
```

## 📊 Структура проекта

```
neridrone111/
├── drone_navigation.py          # 🎯 Главный модуль системы (запуск здесь)
├── stabilization_processor.py  # Модуль стабилизации (Optical Flow)
├── video_processor.py           # Обработчик видеопотока
├── navigation_server.py          # TCP/UDP сервер для связи с дроном
├── gps_integration.py           # Интеграция с GPS/ГЛОНАСС
├── auto_calibration.py          # Автокалибровка при взлете
├── home_return.py               # Модуль возврата домой (RTH)
├── route_monitor.py             # Мониторинг маршрута
├── server.py                    # Веб-сервер (Flask, для отладки)
├── example.py                   # Простой пример
├── requirements.txt             # Зависимости
├── README.md                    # Документация
└── REQUIREMENTS_CHECKLIST.md    # ✅ Чеклист соответствия требованиям
```

## 🔧 Алгоритм

Система использует **Optical Flow (Lucas-Kanade)**:

1. **Детекция ключевых точек** - находит углы и особенности на первом кадре
2. **Отслеживание** - отслеживает эти точки между последовательными кадрами
3. **Вычисление смещения** - медианное смещение всех точек
4. **Фильтрация** - медианный фильтр для устранения выбросов
5. **Накопление** - накапливает смещение для определения общей позиции

## 📈 Метрики

Система возвращает:

- **position** - текущая позиция [x, y]
- **offset** - смещение от начальной точки [dx, dy]
- **velocity** - скорость смещения [vx, vy] (пикселей/кадр)
- **confidence** - уверенность трекинга (0.0-1.0)
- **stability** - метрики стабильности:
  - `is_stable` - стабильна ли позиция
  - `drift_rate` - скорость дрифта
  - `position_variance` - вариация позиции

## 🎛️ Настройка

### Параметры VideoProcessor

```python
VideoProcessor(
    use_dual_camera=False,        # Использовать две камеры
    primary_method='lucas_kanade', # Метод: 'lucas_kanade' или 'farneback'
    secondary_method='farneback',  # Метод для второй камеры
    route_monitor=None             # Монитор маршрута (опционально)
)
```

### Параметры PositionStabilizer

```python
from stabilization_processor import PositionStabilizer

stabilizer = PositionStabilizer(
    method='lucas_kanade',
    max_corners=200,          # Количество точек для отслеживания
    quality_level=0.01,       # Порог качества точек
    min_distance=10           # Минимальное расстояние между точками
)
```

## 🔍 Устранение проблем

### Низкая уверенность (confidence < 0.5)

- Улучшите освещение
- Используйте более текстурированную поверхность
- Проверьте качество видео

### Высокий дрифт

- Используйте Farneback метод (более точный)
- Увеличьте количество точек отслеживания
- Улучшите условия съемки

### Низкий FPS

- Уменьшите `max_corners`
- Используйте меньший размер кадра
- Используйте Lucas-Kanade вместо Farneback

## 📝 Лицензия

Проект создан для учебных целей.

## 🤝 Поддержка

Для вопросов и проблем создайте issue в репозитории проекта.

