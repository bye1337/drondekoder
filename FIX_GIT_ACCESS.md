# Настройка доступа Git для отправки в develop

## ✅ Статус

Изменения успешно смержены в ветку `develop` локально. Готово к отправке:

- ✅ 3 новых коммита в ветке develop
- ✅ Все изменения из develop2.0 включены
- ❌ Нужно настроить доступ для push

## 🔧 Решение проблемы доступа

### Ошибка:
```
ERROR: Permission to NarKomaRick/neridrone111.git denied to bye1337.
```

### Вариант 1: Настроить SSH ключ (рекомендуется)

1. **Проверить существующий ключ:**
```bash
ls -la ~/.ssh/id_rsa.pub
cat ~/.ssh/id_rsa.pub
```

2. **Если ключа нет, создать:**
```bash
ssh-keygen -t ed25519 -C "your-email@example.com"
# Или
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"
```

3. **Скопировать публичный ключ:**
```bash
cat ~/.ssh/id_rsa.pub
```

4. **Добавить ключ на GitHub:**
   - Зайти на https://github.com/settings/keys
   - Нажать "New SSH key"
   - Вставить содержимое `~/.ssh/id_rsa.pub`
   - Сохранить

5. **Проверить подключение:**
```bash
ssh -T git@github.com
# Должно быть: "Hi NarKomaRick! You've successfully authenticated..."
```

6. **Отправить изменения:**
```bash
git push origin develop
```

### Вариант 2: Использовать Personal Access Token (HTTPS)

1. **Изменить remote на HTTPS:**
```bash
git remote set-url origin https://github.com/NarKomaRick/neridrone111.git
```

2. **Создать Personal Access Token:**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выбрать права: `repo` (полный доступ к репозиториям)
   - Скопировать токен

3. **Отправить с токеном:**
```bash
git push origin develop
# Username: NarKomaRick
# Password: <вставить токен>
```

### Вариант 3: Исправить SSH конфигурацию

Если ключ уже есть, но не работает:

1. **Проверить SSH агент:**
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
```

2. **Проверить подключение:**
```bash
ssh -T git@github.com
```

3. **Отправить:**
```bash
git push origin develop
```

## 📊 Что будет отправлено

**65 файлов изменено:**
- ✅ Добавлено: 15 новых модулей (стабилизация, интеграция с Matek F405, GPS, автокалибровка, возврат домой)
- ✅ Удалено: 50 старых файлов (нейросети, обучение, ненужные модули)
- ✅ Изменено: requirements.txt, README.md, документация

**Основные изменения:**
1. Переписан под Optical Flow стабилизацию
2. Интеграция с Matek F405 через MAVLink
3. Автокалибровка при взлете
4. Модуль возврата домой
5. TCP/UDP сервер
6. Документация по развертыванию

## 🚀 После настройки доступа

Просто выполните:
```bash
git push origin develop
```

Изменения отправятся в репозиторий https://github.com/NarKomaRick/neridrone111 на ветку develop.

