# 🚀 Начало работы с системой

## 📋 Понимание сообщения "модель не загружена"

Когда вы открываете веб-интерфейс и видите:
```
⚠️ Модель не загружена
```

Это **НЕ ОШИБКА!** Это нормально, потому что:
- Проект только что создан
- Модель еще не обучена на ваших данных
- Система готова, но нужна обученная модель

## ✅ Что работает СЕЙЧАС без модели?

1. ✅ **Веб-интерфейс** - полностью работает
2. ✅ **Система отслеживания маршрута** - работает
3. ✅ **API endpoints** - все работают
4. ✅ **Интерфейс загрузки файлов** - работает

Что НЕ работает без модели:
- ❌ Реальное определение координат (возвращаются тестовые значения)

## 🎯 Два пути к работающей системе

### Путь 1: Тестирование интерфейса (5 минут) 🚀

Просто запустите веб-интерфейс:

```bash
streamlit run src/api/web_app.py
```

**Что вы сможете сделать:**
- ✅ Открыть интерфейс
- ✅ Загружать изображения
- ✅ Создавать маршруты
- ✅ Тестировать отслеживание маршрута
- ⚠️ Получать тестовые координаты (не реальные)

### Путь 2: Обучение модели (2-4 часа) 🎓

Для получения РЕАЛЬНЫХ координат нужно обучить модель.

#### Шаг 1: Подготовка данных

**Создайте датасет из карты:**

```bash
# Скачайте любую большую спутниковую карту (например, с Google Earth)
# Сохраните её как map.jpg

# Создайте 1000 пар изображений из карты
python scripts/create_dataset.py --map-image map.jpg --num-pairs 1000
```

**Или используйте готовые данные:**
- Загрузите пары в папку `data/processed/pairs/`
- Формат: `image_001_map.jpg` и `image_001_drone.jpg`

#### Шаг 2: Обучение

```bash
python src/training/train.py --config config/train_config.yaml
```

**На что обратить внимание:**
- ⏱️ Время: 2-4 часа на GPU, 12-24 часа на CPU
- 💾 Место: минимум 5GB свободного места
- 🎮 GPU: рекомендуется (ускоряет в 10 раз)

#### Шаг 3: Проверка

После обучения модель сохранится в:
```
models/checkpoints/best_model.pth
```

Перезапустите веб-интерфейс - модель загрузится автоматически!

## 📊 Быстрая проверка системы

Запустите пример, чтобы убедиться, что всё работает:

```bash
python example.py
```

Вы должны увидеть:
```
[SUCCESS] All examples completed successfully!
```

Если видите это - **всё отлично!** Система работает.

## 🎮 Что делать дальше?

1. **Протестируйте интерфейс** (без обучения):
   ```bash
   streamlit run src/api/web_app.py
   ```

2. **Обучите модель** (для реального использования):
   - Подготовьте данные
   - Запустите обучение
   - Используйте в интерфейсе

3. **Изучите документацию**:
   - [QUICKSTART.md](QUICKSTART.md) - быстрые команды
   - [TRAINING_GUIDE.md](TRAINING_GUIDE.md) - детали обучения
   - [INSTALL.md](INSTALL.md) - установка

## ❓ Часто задаваемые вопросы

**Q: Нужна ли обученная модель для запуска?**  
A: Нет! Интерфейс работает без неё для тестирования.

**Q: Сколько нужно данных для обучения?**  
A: Минимум 100-200 пар, лучше 1000+.

**Q: Можно ли использовать CPU?**  
A: Да, но обучение будет в 10 раз медленнее.

**Q: Сколько времени занимает обучение?**  
A: 2-4 часа на GPU, 12-24 часа на CPU.

**Q: Система точно работает?**  
A: Да! Проверьте: `python example.py` - должно завершиться успешно.

## 🆘 Если что-то не работает

1. **Проверьте установку:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Проверьте Python:**
   ```bash
   python --version  # Должно быть 3.8+
   ```

3. **Проверьте PyTorch:**
   ```bash
   python -c "import torch; print(torch.__version__)"
   ```

4. **Запустите пример:**
   ```bash
   python example.py
   ```

Если пример работает - всё в порядке!

---

## ✅ Итог

**Текущий статус:** ✅ Система полностью работает и готова к использованию

**Модель:** ⚠️ Требует обучения для реального позиционирования (но интерфейс работает без неё!)

**Рекомендация:** Начните с тестирования интерфейса, затем обучите модель при необходимости.

**Удачи!** 🚀

